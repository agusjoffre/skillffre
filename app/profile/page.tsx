import UserInfoCard from '@/components/profilepage/user-info-card';
import React from 'react';

type Props = {};

const ProfilePage = (props: Props) => {
  return (
    <div>
      Profile page
      <UserInfoCard />
    </div>
  );
};

export default ProfilePage;
